% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aou_create_bucket.R
\name{aou_create_bucket}
\alias{aou_create_bucket}
\title{Create a Cloud Storage bucket for the workspace}
\usage{
aou_create_bucket(
  temporary = FALSE,
  name = if (temporary) "temporary-workspace-bucket" else "workspace-bucket",
  auto_delete = 14
)
}
\arguments{
\item{temporary}{If \code{TRUE}, creates a temporary bucket whose contents are
automatically deleted after \code{auto_delete} days, instead of the
persistent workspace bucket. Useful for intermediate files you don't
need to keep.}

\item{name}{Resource name for the bucket within the workspace. Defaults to
\code{"workspace-bucket"}, or \code{"temporary-workspace-bucket"} if
\code{temporary = TRUE}.}

\item{auto_delete}{Number of days after which objects in a temporary
bucket are automatically deleted. Only used if \code{temporary = TRUE}.}
}
\value{
The bucket's \verb{gs://} URL (invisibly).
}
\description{
Verily Workbench workspaces (workbench 2.0) don't
automatically provision a workspace bucket the way older ("workbench
1.0") All of Us workspaces did. This creates one via the \code{wb} CLI (or
resolves it if a bucket with the same name already exists) and sets
\code{WORKSPACE_BUCKET} (or \code{WORKSPACE_TEMP_BUCKET} if \code{temporary = TRUE}) for
the current R session, so functions like \code{aou_ls_bucket()} work without
further setup.
}
\details{
The resolved bucket URL is cached to \verb{~/.aou-env} (alongside
\code{WORKSPACE_CDR} and \code{GOOGLE_PROJECT}, which are cached there the same
way when the package loads). This means you only need to run
\code{aou_create_bucket()} once per workspace, not once per session: on
subsequent loads, \code{library(allofus)} reads the cached values from
\verb{~/.aou-env} directly instead of recreating anything or re-querying the
\code{wb} CLI.

To see what's currently cached (e.g., to check which buckets you've
already created in this workspace), read the file directly:
\code{cat(readLines("~/.aou-env"))}. Each line is a plain
\verb{export VARIABLE="value"} entry. If a cached value ever goes stale (for
example, a bucket was deleted outside of R), delete the corresponding
line, or delete the file entirely to force everything to be re-resolved
on the next \code{library(allofus)} call.
}
\examples{
\dontshow{if (on_workbench()) withAutoprint(\{ # examplesIf}
aou_create_bucket()
aou_create_bucket(temporary = TRUE)
\dontshow{\}) # examplesIf}
}
